apiVersion: v1
kind: Service
metadata:
  name: demo-server-service-front
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: demo-server-front