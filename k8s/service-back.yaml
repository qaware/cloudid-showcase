apiVersion: v1
kind: Service
metadata:
  name: demo-server-service-back
spec:
  type: NodePort
  ports:
    - name: http
      port: 81
      targetPort: 8080
      protocol: TCP
  selector:
    app: demo-server-back