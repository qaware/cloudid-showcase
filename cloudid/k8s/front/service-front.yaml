apiVersion: v1
kind: Service
metadata:
  name: cloudid-service-front
spec:
  type: NodePort
  ports:
    - name: http
      port: 443
      targetPort: 8443
      protocol: TCP
  selector:
    app: cloudid-front